import React from "react";
import IconBox from "../../atoms/group3/IconBox";

const ContactInfoCard = ({ icon, title, text }) => (
    <IconBox icon={icon} title={title} text={text} />
);

export default ContactInfoCard;
